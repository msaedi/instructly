# backend/pyproject.toml
[tool.black]
line-length = 120
target-version = ['py39', 'py310', 'py311']
include = '\.pyi?$'
extend-exclude = '''
/(
  \.git
  | \.mypy_cache
  | \.tox
  | \.venv
  | venv
  | _build
  | buck-out
  | build
  | dist
  | alembic/versions
)/
'''

[tool.isort]
profile = "black"
line_length = 120
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
skip_glob = ["*/alembic/versions/*", "*/migrations/*"]
known_first_party = ["app"]
sections = ["FUTURE", "STDLIB", "THIRDPARTY", "FIRSTPARTY", "LOCALFOLDER"]

[tool.coverage.run]
source = ["app"]
branch = true
data_file = ".coverage"
parallel = false
omit = [
    "*/tests/*",
    "*/test_*.py",
    "*/__init__.py",
    "*/migrations/*",
    "*/alembic/*",
    "app/main.py",
    "app/database.py",
    "app/core/config.py",
]

[tool.coverage.report]
precision = 2
skip_covered = false
show_missing = true
skip_empty = true
exclude_lines = [
    # Have to re-enable the standard pragma
    "pragma: no cover",

    # Don't complain about missing debug-only code
    "def __repr__",
    "if self\\.debug",

    # Don't complain if tests don't hit defensive assertion code
    "raise AssertionError",
    "raise NotImplementedError",

    # Don't complain if non-runnable code isn't run
    "if 0:",
    "if __name__ == .__main__.:",

    # Don't complain about abstract methods
    "@(abc\\.)?abstractmethod",

    # Ignore type checking blocks
    "if TYPE_CHECKING:",

    # Ignore ellipsis in abstract methods/stubs
    "\\.\\.\\.",
]

[tool.coverage.html]
directory = "htmlcov"

[tool.coverage.xml]
output = "coverage.xml"

[tool.coverage.json]
output = "coverage.json"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = """
    -v
    --tb=short
    --strict-markers
    --cov-config=pyproject.toml
"""
filterwarnings = [
    "ignore::urllib3.exceptions.NotOpenSSLWarning",
    "ignore::DeprecationWarning",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
    "requires_cache: marks tests that require cache to be running",
    "supporting_systems: marks tests for supporting systems",
]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [
  "backend/tests/**",
  "backend/**/tests/**",
  "backend/migrations/**",
  "backend/alembic/**",
  "backend/scripts/**"
]
src = ["backend/app"]

[tool.ruff.lint]
select = ["F","I"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["app"]
combine-as-imports = true
force-sort-within-sections = true
force-single-line = false

[tool.ruff.lint.per-file-ignores]
"**/migrations/**" = ["ALL"]
"backend/tests/**/*" = ["ALL"]
"backend/app/__init__.py" = ["F401"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
warn_unused_ignores = true
no_implicit_optional = true
warn_redundant_casts = true
warn_return_any = true
disallow_untyped_defs = true
explicit_package_bases = true
follow_imports = "skip"
plugins = ["pydantic.mypy"]
mypy_path = "typings"
files = [
  "app/schemas/**",
  "app/routes/internal.py",
  "app/services/base.py",
  "app/services/stripe_service.py",
]
exclude = [
  "tests",
  "migrations",
  "scripts"
]

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.internal",
  "backend.app.schemas.base",
  "backend.app.schemas.search_history_responses"
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.uploads"
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.messages",
  "backend.app.routes.users_profile_picture",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.gated",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.availability_windows",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.search_history",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.addresses",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.services",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.instructors",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.beta",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.payments",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.bookings",
]
strict = true
[[tool.mypy.overrides]]
module = [
  "backend.app.schemas.review",
]
strict = true
[[tool.mypy.overrides]]
module = [
  "backend.app.schemas.message_requests",
]
strict = true
[[tool.mypy.overrides]]
module = [
  "backend.app.schemas.payment_schemas",
]
strict = true
[[tool.mypy.overrides]]
module = [
  "backend.app.services.base",
]
follow_imports = "normal"
check_untyped_defs = true
warn_unused_ignores = true
no_implicit_optional = true

[[tool.mypy.overrides]]
module = [
  "backend.app.services.stripe_service",
]
follow_imports = "normal"
check_untyped_defs = true
disallow_incomplete_defs = true
no_implicit_optional = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = [
  "backend.app.schemas.*"
]
check_untyped_defs = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = [
  "backend.app.repositories.availability_repository",
  "backend.app.repositories.beta_repository",
  "backend.app.repositories.booking_repository",
  "backend.app.repositories.factory",
  "backend.app.repositories.favorites_repository",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.repositories.bulk_operation_repository",
  "backend.app.repositories.conflict_checker_repository",
  "backend.app.repositories.region_boundary_repository",
  "backend.app.repositories.search_interaction_repository",
  "backend.app.repositories.slot_manager_repository",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.repositories.address_repository",
  "backend.app.repositories.review_repository",
  "backend.app.repositories.search_event_repository",
  "backend.app.repositories.search_history_repository",
  "backend.app.repositories.week_operation_repository",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.repositories.instructor_profile_repository",
  "backend.app.repositories.message_repository",
  "backend.app.repositories.payment_repository",
  "backend.app.repositories.rbac_repository",
  "backend.app.repositories.service_catalog_repository",
  "backend.app.repositories.user_repository",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.services.message_notification_service",
  "backend.app.services.personal_asset_service",
  "backend.app.services.search_history_cleanup_service",
  "backend.app.services.template_service",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.database_monitor",
  "backend.app.routes.favorites",
  "backend.app.routes.privacy",
  "backend.app.routes.redis_monitor",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.services.device_tracking_service",
  "backend.app.services.two_factor_auth_service",
  "backend.app.services.week_operation_service",
  "backend.app.tasks.email",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.alerts",
  "backend.app.routes.analytics",
  "backend.app.routes.codebase_metrics",
  "backend.app.routes.monitoring",
  "backend.app.routes.two_factor_auth",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.services.cache_service",
  "backend.app.services.instructor_service",
  "backend.app.tasks.analytics",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "backend.app.routes.metrics",
  "backend.app.routes.prometheus",
]
strict = true

[[tool.mypy.overrides]]
module = [
  "pydantic",
  "pydantic.*"
]
ignore_missing_imports = false
follow_imports = "normal"
