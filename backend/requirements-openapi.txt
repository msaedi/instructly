# Minimal deps to import the FastAPI app & build schema
fastapi>=0.115
starlette>=0.40
pydantic>=2
typing-extensions>=4.12

# OpenAPI JSON minifier
orjson>=3.10,<4

# Safety net: if a route still imports SQLAlchemy at runtime
SQLAlchemy>=2.0

# Ensure metrics & rate-limit backend resolvable when app imports modules
prometheus-client>=0.20
redis>=5
