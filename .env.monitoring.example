# Grafana Admin Credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_password_here

# Slack Notifications (Optional)
# To enable Slack notifications:
# 1. Create an incoming webhook in your Slack workspace:
#    - Go to https://api.slack.com/apps
#    - Create a new app or select existing
#    - Add "Incoming Webhooks" feature
#    - Create webhook for your alerts channel
# 2. Copy the webhook URL below
# If not set, alerts will only appear in Grafana UI
SLACK_WEBHOOK_URL=

# Email Notifications (Optional)
# Comma-separated list of email addresses for alert notifications
ALERT_EMAIL_ADDRESSES=oncall@example.com,engineering@example.com

# PagerDuty Integration (Optional)
# For critical alerts escalation
PAGERDUTY_INTEGRATION_KEY=

# Note: Change these values before running the monitoring stack
# These credentials will be used to log into Grafana at http://localhost:3003
