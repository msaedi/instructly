name: Frontend lint & build

on:
  workflow_dispatch:

jobs:
  lint-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: '22.x'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: frontend
      - name: ESLint (full project)
        run: npm run lint
        working-directory: frontend
      - name: ESLint (CI guardrail)
        run: npm run lint:ci
        working-directory: frontend
      - name: Typecheck (default config)
        run: npm run typecheck
        working-directory: frontend
      - name: Typecheck (strict)
        run: npm run typecheck:strict
        working-directory: frontend
      - name: Typecheck (strict-all)
        run: npm run typecheck:strict-all
        working-directory: frontend
      - name: Next build (capture warnings)
        run: |
          mkdir -p .artifacts
          npm run build:log || true
          node scripts/check-next-warnings.js
        working-directory: frontend
        env:
          WARNING_BUDGET: "0"
          NEXT_PUBLIC_API_BASE: http://localhost:8000
          NEXT_PUBLIC_APP_ENV: ci
          NEXT_PUBLIC_USE_PROXY: 'false'
          NEXT_PUBLIC_APP_URL: http://localhost:3100
          NEXT_PUBLIC_LOG_LEVEL: error
          LOG_LEVEL: error
      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: next-build-log
          path: frontend/.artifacts/next-build.log
