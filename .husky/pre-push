#!/bin/sh

. "$(dirname "$0")/_/husky.sh"

# Frontend strict typecheck gate before push (fast, no emit)
if [ -d "frontend" ]; then
  echo "Running frontend strict typecheck..."
  (cd frontend && npm run typecheck:strict) || exit 1
fi
