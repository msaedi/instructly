#!/bin/sh

# Frontend strict typecheck gate before push (full, no emit)
if [ -d "frontend" ]; then
  echo "🧠 Running frontend strict typecheck (full)..."
  (cd frontend && npm run typecheck:strict-all) || exit 1
fi
